Install dependencies

pip install ultralytics
pip install pandas
pip install streamlit
pip install flask
pip install flask_cors
pip install cv2

To run the project, run these commands in three different terminals in the given order

python backend_server.py
python vehicle_counter_video.py
streamlit run new.py